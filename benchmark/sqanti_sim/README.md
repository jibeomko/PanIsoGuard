# SQANTI-SIM AUPRC sweep placeholder

Status: not run. This directory is reserved for the planned SQANTI-SIM threshold
sweep described in `docs/validation.md`. Current truth-based validation is covered
by `benchmark/controlled_truth` and `benchmark/end2end`.
